sudo: required

services:
  - docker

notifications:
  slack: 
    on_success: never
    on_failure: always
    rooms:
      - oicr:S9k4EowgQv9AnbCfEZHSzCsg

language: java
jdk: 
  - oraclejdk8

before_install:
  - docker build -t quay.io/collaboratory/dockstore-tool-bwa-mem .
  - pip2.7 install --user setuptools==24.0.3

install: 
  - pip2.7 install --user cwl-runner

script: 
  - cwltool Dockstore.cwl test.json 
